# SpecBox /stripe-connect — pubspec fragment
# /stripe-connect merges these keys into pubspec.yaml → dependencies and then
# removes this file. Versions are pinned to majors to allow patch updates.
dependencies:
  flutter_stripe: ^10.0.0
  url_launcher: ^6.2.0
  # If your project uses Riverpod:
  flutter_riverpod: ^2.5.0
  # If your project uses Dio for HTTP (most projects do; skip otherwise):
  dio: ^5.4.0
