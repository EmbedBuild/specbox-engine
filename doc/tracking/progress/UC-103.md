# [UC-103] Seleccion de backend Nativo opt-in por proyecto

- **Estado**: Review
- **US padre**: US-NATIVE-BACKEND
- **Horas estimadas**: 6
- **Actor**: Dev configurando proyecto

## Acceptance Criteria

| AC | Descripción | Estado |
|----|-------------|--------|
| AC-07 | [AC-07] set_auth_token(backend_type='native') registra el proyecto contra Postgres; detect_project_backend devuelve 'native' cuando specbox.backend_type='native' esta en settings.local.json, con prioridad sobre senales de filesystem. | DONE |
| AC-08 | [AC-08] Con backend Nativo, import_spec persiste US/UC/AC en Postgres y list_us/list_uc la recuperan identica (round-trip campo a campo). | DONE |
| AC-09 | [AC-09] Un proyecto Trello/Plane/FreeForm sigue funcionando sin cambios cuando NativeBackend esta disponible (suite de regresion de los 3 backends en verde). | DONE |

## Evidencia

- `server/auth_gateway.py` — rama `native` + `store_native_credentials` (Frontera 2: solo `project_id` en sesión).
- `server/tools/spec_driven.py` — `set_auth_token` con kwarg `project_id`.
- `server/app_docs/discovery.py` — `detect_project_backend` prioriza `native`.
- `tests/test_native_dispatch.py` — 8 tests (resolución, store, detect, no-regresión, round-trip).
- Cambios aditivos: Trello/Plane/FreeForm sin cambios (AC-09 verificado).
- Nota CI: el round-trip exige `SPECBOX_NATIVE_DSN` en env; skipea limpio si falta.
