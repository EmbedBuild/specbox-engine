# UC-630: Cablear init_observability + add_middleware condicional en server.py (opt-in por env var)

- **Estado**: Backlog
- **US padre**: US-MCP-OBSERVABILITY
- **Horas estimadas**: 1.0
- **Actor**: Engine

## Acceptance Criteria

| AC | Descripción | Estado |
|----|-------------|--------|
| AC-01 | [AC-01] Con `SPECBOX_OBSERVABILITY_ENABLED=true`, tras `import server.server`, `mcp._middlewares` (o equivalente API publica de FastMCP) contiene 1 instancia de `MCPObservabilityMiddleware`; test integration verifica. | PENDING |
| AC-02 | [AC-02] Con `SPECBOX_OBSERVABILITY_ENABLED` ausente o `=false`, tras `import server.server`, `opentelemetry` NO esta en `sys.modules` y `mcp._middlewares` no contiene `MCPObservabilityMiddleware`; test verifica ambas condiciones. | PENDING |
| AC-03 | [AC-03] Si `init_observability()` levanta excepcion (ej. OTLP endpoint invalido), el server termina de cargarse sin crashear y emite log `mcp_observability_init_failed` con el error; test con monkeypatch fuerza la excepcion y verifica que `import server.server` no levanta. | PENDING |
| AC-04 | [AC-04] Con observabilidad activa, log inicial `mcp_observability_enabled` incluye campo `exporter` con el valor de `OTEL_EXPORTER_OTLP_ENDPOINT` (o `'console'` si no esta seteado); test captura log output. | PENDING |
