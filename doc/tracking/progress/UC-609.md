# UC-609: Control Panel — UI: ruta /claims → /reservations, ClaimsPage, sidebar, breadcrumbs, copy

- **Estado**: Backlog
- **US padre**: US-CLAIM-RENAME
- **Horas estimadas**: 0
- **Actor**: ControlPanel

## Acceptance Criteria

| AC | Descripción | Estado |
|----|-------------|--------|
| AC-01 | [AC-01] [AC-01] Navegar a `https://specbox-controlpanel.embed.build/reservations` (o `pnpm dev` local) renderiza la lista de reservas activas con título `"Reservas activas"`; navegar a `/claims` redirige a `/reservations` automáticamente (301 / Navigate replace). | PENDING |
| AC-02 | [AC-02] [AC-02] El sidebar muestra el item `"Reservas"` con el icono renombrado; ningún texto visible en la UI muestra la palabra "claim" o "Claim" o "claims" (verificado con búsqueda manual y con un script `grep -rni 'claim' apps/web/src/` que devuelve 0 hits fuera de comentarios JSDoc que mencionan el rename histórico). | PENDING |
| AC-03 | [AC-03] [AC-03] `pnpm --filter @specbox-panel/web test` termina en verde (los tests unitarios y de snapshot se actualizan a los nuevos nombres) y `tsc -b` no reporta errores de tipos. | PENDING |
| AC-04 | [AC-04] [AC-04] El componente `<ActivityFeed>` renderiza las dos copias nuevas: "reservó" y "liberó la reserva de" — verificado con un test de snapshot. | PENDING |
