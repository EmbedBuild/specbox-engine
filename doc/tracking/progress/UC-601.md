# UC-601: Migración SQL: renombrar uc_claims → uc_reservations en specbox-engine

- **Estado**: In Progress
- **US padre**: US-CLAIM-RENAME
- **Horas estimadas**: 0
- **Actor**: Engine

## Acceptance Criteria

| AC | Descripción | Estado |
|----|-------------|--------|
| AC-01 | [AC-01] [AC-01] La migración `0007_rename_claims_to_reservations.sql` existe, es idempotente (re-aplicarla dos veces no falla), y al aplicarse renombra `uc_claims` → `uc_reservations`, `claimed_at` → `reserved_at`, y el índice de developer_id; las filas pre-existentes se conservan (verificado insertando una fila antes, aplicando la migración, y haciendo SELECT que la recupere con los nuevos nombres). | PASS |
| AC-02 | [AC-02] [AC-02] Tras aplicar la migración, `psql \d uc_reservations` muestra la tabla con la PK `(project_id, uc_id)`, FKs ON DELETE CASCADE hacia `projects(project_id)` y `developers(developer_id)`, y la columna `reserved_at TIMESTAMPTZ NOT NULL DEFAULT now()`. La tabla `uc_claims` ya no existe (`SELECT to_regclass('uc_claims')` devuelve NULL). | PASS |
| AC-03 | [AC-03] [AC-03] La migración puede aplicarse a un Postgres dev local levantado desde cero (sin filas previas) y a un Postgres con datos pre-migración sin warnings ni errores. | PASS |

## Comentarios

- **2026-05-24T12:25:31.278676+00:00**: Desarrollo iniciado: 2026-05-24T12:25:31.278648+00:00
